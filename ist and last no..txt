 #include <stdio.h>
#include<stdlib.h>
#include<string.h>
int main(){
	int i,l;
char str[10];
scanf("%s",str);
l=strlen(str);
for(i=0;i<1;i++)
{
printf("%c %c",str[0],str[l-1]);
}
      // Writing output to STDOUT
}